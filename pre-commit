#!/bin/sh

# run tests
TEST_RESULT = python -m unittest discover -s LinkChecker

# run pyflakes
PYFLAKES_RESULT = pyflakes .

# python test runner will set exit code appropriately
if (TEST_RESULT || PYFLAKES_RESULT)
then
  exit 1
else
  exit 0
